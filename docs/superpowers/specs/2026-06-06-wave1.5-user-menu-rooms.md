# Wave 1.5 — User Menu + Account Rooms

**Date:** 2026-06-06  
**Status:** Approved  
**Scope:** User menu cleanup, Billing wrapper migration, Family avatar fix, Settings audit

---

## 1. Context

Wave 1 restructured the bottom nav (cloth | memory | ∞ | letter | voice) and built LetterRoom and VoiceRoom. The user menu in `Frame.tsx` was not touched in Wave 1. It now has two problems:

1. **Content links in an account menu.** "Memories" and "Ask the thread" belong to content browsing — the cloth/tapestry view now owns that territory. Keeping them in the account dropdown creates two surfaces for the same thing.
2. **Billing uses the wrong shell.** Settings and Family already use `ClothShell`; Billing uses `Frame`. The inconsistency is visible: Billing has the full topbar with the entry counter and user menu, while the other account pages have a minimal topbar with a back link.

Additionally, `routeLabel()` in `Frame.tsx` is missing the three Wave 1 routes, so those pages show "heirloom" as the topbar label instead of the correct contextual label.

---

## 2. User Menu (Frame.tsx)

### After

Three items only, all lowercase, collapsed to a single section before sign out:

```
[user name]
[user email]
─────────────
family
settings
billing
─────────────
sign out
```

### Changes

- Remove: `Memories` → `/memories`
- Remove: `Ask the thread` → `/ask`
- Rename: `Family` → `family` (lowercase)
- Rename: `Settings` → `settings` (lowercase)
- Rename: `Billing` → `billing` (lowercase)
- Rename: `Sign out` → `sign out` (lowercase)
- Collapse: was three sections (content | account | sign out), becomes two (account | sign out)
- No changes to the button trigger (initials, 32×32, 0px radius, monospace) — it is already spec-compliant

### routeLabel() additions

Add to `routeLabel()` before the catch-all `return 'heirloom'`:

```typescript
if (pathname.startsWith('/loom/letter'))         return 'letter';
if (pathname.startsWith('/loom/voice'))          return 'voice';
if (pathname.startsWith('/loom/compose-letter')) return 'compose letter';
```

---

## 3. Billing Room (`/billing`)

### Problem

`Billing.tsx` uses `Frame` as its shell. `Frame` renders the full topbar (HLogo wordmark + entry counter + theme toggle + security dot + user menu). This is the correct shell for the Tapestry view — it is not appropriate for a focused account subpage where the user came from the user menu.

### Fix

Replace `Frame` with `ClothShell`. Topbar:

- Left: `← heirloom` — a `<Link to="/loom">` in monospace uppercase, bone-faint
- Center: `billing`

All billing content and functionality is preserved unchanged — only the shell wrapper changes.

### Constraints

- `ClothShell` does not render the entry counter or user menu — this is correct for an account subpage
- Remove the `Frame` import; add `ClothShell` import from `../loom/components/ClothShell`
- Remove any `Frame`-specific props (`left=`, `right=`, `showEdge=`)

---

## 4. Family Room (`/family`)

### Problem

`Family.tsx` uses `ClothShell` (correct) and the `DYE_VARS` / `DYE_TEXT` system (correct). However, the `FamilyMember` interface has an `avatarUrl` field, and the page may render avatar circles — a §2.6 anti-pattern.

### Fix

Audit `Family.tsx` for avatar circle rendering. Replace any `avatarUrl`-based circle (typically a `border-radius: 50%` or `rounded-full` element) with:

- **3px solid dye left-border** on the member row — use `member.dye ? DYE_VARS[member.dye] : DYE_VARS['iron']`
- **Member name in DYE_TEXT color** — `member.dye ? DYE_TEXT[member.dye] : 'var(--bone-dim)'`
- The `avatarUrl` field can remain on the interface (it comes from the API) but must not be rendered

### Member row structure after fix

```
[3px dye border] [name in DYE_TEXT color]  ·  [relationship]      [edit]
                 [email if present]
                 [role/status if present]
```

---

## 5. Settings Audit (`/settings`)

### Problem

`Settings.tsx` uses `ClothShell` and is generally well-structured. A targeted pass is needed to catch any design constitution violations introduced during the settings feature build.

### What to check and fix

1. **Border-radius** — all `borderRadius` values must be 0px. Input fields max 2px. Never 4px+.
2. **Inline hex colors** — any raw hex (`#b07a4a`, `#f4ecd8`, etc.) must be replaced with CSS tokens (`var(--warm)`, `var(--bone)`, etc.). Exception: values not in the token set (rare) may stay.
3. **Spinners** — any spinner/loader must be replaced with a 1px hairline progress bar (`background: var(--warm), height: 1px, position: fixed top`).
4. **Toasts** — any toast/snackbar must be replaced with inline status text.
5. **`savedFlash` feedback** — check the existing `savedFlash` state renders inline (e.g., a monospace "saved" line below the button), not as a toast.

If no violations are found, no changes are made and the task is recorded as "no changes needed."

---

## 6. Files to modify

| File | Change |
|---|---|
| `cloudflare/frontend/src/loom/components/Frame.tsx` | Remove Memories/Ask links, lowercase menu items, collapse sections, add routeLabel entries |
| `cloudflare/frontend/src/pages/Billing.tsx` | Replace `Frame` wrapper with `ClothShell` |
| `cloudflare/frontend/src/pages/Family.tsx` | Remove avatar circle rendering, replace with dye left-border + DYE_TEXT name |
| `cloudflare/frontend/src/pages/Settings.tsx` | Design constitution audit — fix any violations found |

---

## 7. Out of scope

- Memories page (`/memories`) — not in the new menu; no changes
- Ask page (`/ask`) — removed from menu; no changes to the page itself
- Route changes — all subpages keep their existing URLs
- Functional changes to Settings, Billing, or Family
- The BottomNav — already completed in Wave 1
